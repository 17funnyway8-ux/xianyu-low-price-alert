# ===========================================================
# Web 基础服务依赖（P1，仅容器/Web 需要）
# 桌面版 requirements.txt 保持不变（保护 exe 体积）；
# 不装 PySide6 / tkinter / playwright —— 零新增桌面依赖。
# 纯 uvicorn 即可（不强制 [standard]，避免 uvloop/httptools 增量）。
# ===========================================================
fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0
